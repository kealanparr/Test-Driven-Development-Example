Hi,

This is code written for the Test-Driven article!

To run the solution you can simply run
`node index.js` and all the tests will be automatically run.

If your tests fail, you will get assertions.
